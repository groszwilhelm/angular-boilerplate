{
  "name": "webtech-and-dragons",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "3.1.0"
  },
  "scripts": {
    "start": "npm --prefix client start",
    "build:prod": "npm run --prefix client build:prod",
    "serve:prod": "npm --prefix server start",
    "install": "npm --prefix server install && npm --prefix client install",
    "uninstall": "npm --prefix server uninstall && npm --prefix client uninstall"
  }
}
